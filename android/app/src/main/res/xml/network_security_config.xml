<?xml version="1.0" encoding="utf-8"?>
<!--
  The uploaded payload is already AES-256-GCM encrypted on-device (see
  EncryptedUploader), so plaintext HTTP transport is acceptable for a
  self-hosted LAN server. We still default to HTTPS-only and only permit
  cleartext for development hosts: the emulator's host loopback (10.0.2.2)
  and localhost. To upload over plain HTTP to a LAN IP, add that IP here.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <!-- Example LAN server — uncomment and edit to test over your network:
        <domain includeSubdomains="true">192.168.1.50</domain>
        -->
    </domain-config>
</network-security-config>
