# Runtime dependencies — mirrors [project.dependencies] in pyproject.toml.
# Used by the Docker image for a simple, lockfile-free install.
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pyyaml>=6.0
httpx>=0.27
cryptography>=42.0
