name: dev_archive_notification

# Offline smoke-test pipeline: no `extract.schema`, so the LLM is never called.
# Archives every captured notification to a local file. Use it to verify the
# NotificationListenerService -> upload -> server chain end to end.

trigger:
  signal_type: notification

actions:
  - type: file
    path: ./data/archive/notifications.jsonl
